import { useEffect } from "react";
// import * as SignalClient from "./ts"

export default function SignalComponent() {
    useEffect(() => {
        // const test = SignalClient.PrivateKey.generate().serialize().toString()
        // console.log(test)
    })
   
    return (
        <div>
            {"ok"}
        </div>
    );
}